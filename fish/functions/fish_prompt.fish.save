function fish_prompt
    set_color green
    echo -n (hostname) " => "
    set_color white
    echo -n (prompt_pwd)
    echo
    echo -n "|"
    set_color blue
    echo -n "@"(whoami)
    set_color white
